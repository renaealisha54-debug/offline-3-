// StorageAccessFramework.java - SAF example
import android.app.Activity;
import android.content.Intent;
import android.net.Uri;
import android.provider.DocumentsContract;

public class StorageAccessFramework {
    private static final int REQUEST_OPEN_DOCUMENT = 1001;

    public static void openFile(Activity activity) {
        Intent intent = new Intent(Intent.ACTION_OPEN_DOCUMENT);
        intent.addCategory(Intent.CATEGORY_OPENABLE);
        intent.setType("*/*"); // or "text/plain", "application/json"
        activity.startActivityForResult(intent, REQUEST_OPEN_DOCUMENT);
    }

    // Handle result in onActivityResult
    // Uri uri = data.getData();
    // Then use ContentResolver to read/write
}
